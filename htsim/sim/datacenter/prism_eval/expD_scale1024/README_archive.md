# Six-panel 1024-scale archive

The two CSVs in `archive_data/` contain all D1 and D3 displayed points and population-standard-deviation error bars. Run `python3 archive_fig_data.py --render` from this directory to redraw the six selected panels without simulation logs.
