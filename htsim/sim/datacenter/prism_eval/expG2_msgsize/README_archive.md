# Selected ExpG2 figure archive

`archive_data/figH_a2a_msgsize.csv` and `figH_bfly_msgsize.csv` retain exactly
the plotted geometric-mean CCT slowdowns, asymmetric geometric-SEM error bars,
and completion markers.  The source flow logs are not required to redraw the
two selected panels.

```bash
python3 archive_fig_data.py --render
```
